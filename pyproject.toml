[tool.poetry]
name = "grype-db"
version = "0.1.0"
description = ""
authors = ["Alex Goodman <alex.goodman@anchore.com>"]
readme = "README.md"
packages = [{include = "grype_db"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
