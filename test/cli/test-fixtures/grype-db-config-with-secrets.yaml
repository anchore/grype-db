provider:
  root: ./data

  vunnel:
    executor: local
    env:
      GITHUB_TOKEN: topsy-kretts
      NVD_API_KEY: topsy-kretts

  configs:

    - name: oracle
      type: external
      config:
        env:
          SECRET: topsy-kretts

    - name: oracle
      type: vunnel
      config:
        env:
          SECRET: topsy-kretts
